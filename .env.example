# Session secret for cookie signing (generate a random string)
SESSION_SECRET=your-secret-here

# Simple password for app authentication
APP_PASSWORD=your-password-here

# Port to run on (default 3000)
PORT=3000
